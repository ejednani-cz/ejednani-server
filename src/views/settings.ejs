<!DOCTYPE HTML>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <% include partials/head %>
</head>

<body>
    <div id="header">
        <% include partials/header %>
    </div>

    <div id="container">
        <% include partials/menu %>
    
        <div class="rightCol">
            <div class="subpageBox">
                <div class="subpageContent settings">
                    <h1><%= settings.title %></h1>
                    <h2><%= settings.global %></h2>
                    <p>
                        <input type="checkbox" name="allowNotifications" disabled="disabled" id="settingsAllowNofitications" />
                        <span><label for="settingsAllowNofitications"><%= settings.allowNotifications %></label></span>
                    </p>

                    <h2><%= settings.insideRoom %></h2>
                    <p>
                        <input type="checkbox" name="useSpeaker" disabled="disabled" id="settingsUseSpeaker" />
                        <span><label for="settingsUseSpeaker"><%= settings.useSpeaker %></label></span>
                    </p>
                    
                    <p>
                        <input type="checkbox" name="useMicrophone" disabled="disabled" id="settingsUseMicrophone" />
                        <span><label for="settingsUseMicrophone"><%= settings.useMicrophone %></label></span>
                    </p>

                    <p>
                        <input type="checkbox" name="useCamera" disabled="disabled" id="settingsUseCamera" />
                        <span><label for="settingsUseCamera"><%= settings.useCamera %></label></span>
                    </p>
                </div>
            </div>
        </div>
    </div>

    <script type="text/javascript">
        $.switcher();
    </script>

    <div id="footer">
        <% include partials/footer %>
    </div>
</body>
</html>
